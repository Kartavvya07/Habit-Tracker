/// String constants for route paths and route names across the app.
abstract class RouteNames {
  /// Root initial path / splash status path
  static const String root = '/';

  /// Home / dashboard path (Phase 3 placeholder route target)
  static const String home = '/home';
}
